apiVersion: v1
kind: Service
metadata:
  name: dstack-service
  labels:
    app: dstack
spec:
  type: NodePort
  selector:
    app: dstack
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 30080
